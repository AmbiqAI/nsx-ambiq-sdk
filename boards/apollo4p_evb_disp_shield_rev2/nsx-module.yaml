schema_version: 1

module:
  name: nsx-board-apollo4p-evb-disp-shield-rev2
  type: board
  version: "0.1.0"

support:
  ambiqsuite: true
  zephyr: false

build:
  cmake:
    package: nsx_board_apollo4p_evb_disp_shield_rev2
    targets:
      - nsx::board_apollo4p_evb_disp_shield_rev2

depends:
  required:
    - nsx-ambiq-hal
    - nsx-ambiq-bsp
    - nsx-soc-hal
    - nsx-cmsis-startup
    - nsx-ambiqsuite
  optional: []

constraints:
  required_sdk_provider: nsx-ambiqsuite

compatibility:
  boards:
    - apollo4p_evb_disp_shield_rev2
  socs:
    - apollo4p
  toolchains:
    - arm-none-eabi-gcc
