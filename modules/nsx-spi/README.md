# nsx-spi

`nsx-spi` provides the NSX wrapper for Ambiq SPI usage.

Purpose:
- expose the `nsx_spi_*` API surface
- normalize family-specific and board-specific SPI setup behind one module
- provide an NSX-managed SPI layer for sensor and device modules

Dependencies:
- `nsx-core`
- `nsx-core`
